[package]
name = "webapp"
version = "1.0.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
urlencoding = "2.1"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
